---
import {login} from '../api/auth.ts'

const json = login(import.meta.env.USERNAME, import.meta.env.PASSWORD)
---

<h2 class="text-transparent bg-clip-text bg-gradient-to-r from-red to-red">{json}</h2>


